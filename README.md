# Star Visibility

In the <tt>input.py</tt> file, check the inputs and input a location, target(s), and observing date.

In the terminal, run:

<center><tt>python object_visibility.py</tt></center>


to see star visibility for the chosen observing night (either a date in the form 'YYYY-MM-DD' or just the string 'now').
